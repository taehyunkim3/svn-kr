# svn-mac 세션 인계 메모 (2026-08-26)

## 현재 상태
- `master` = `15ddb22`, origin 에 push 완료. 테스트 558개 3회 연속 통과, 빌드 무경고.
- 세션 시작 시점 테스트 344개 -> 558개.

## 지금 돌고 있는 것 — 전수 커버리지 감사 12개
소스 90파일(생성물 `LocalizationKey.swift` 제외) 22,594줄. 모든 블록을 2명이 본다. 단독 검수 없음.
브리프: `/tmp/svnmac-briefs/sweep-*.txt`, 블록 정의: `/tmp/svnmac-briefs/blocks.json`
보고서 도착 위치: 각 worktree 의 `Docs/audit/2026-08-26-sweep-<블록>-<모델>.md`

| 블록 | 파일 | 줄 | 담당 |
|---|---|---|---|
| B1 SVNClient·스냅샷·경로정규화 | 5 | 4005 | claude, codex |
| B2 ProjectStore 계층 | 17 | 5815 | codex, agy |
| B3a View 전반 | 15 | 2921 | cursor, agy |
| B3b View 후반 | 15 | 4026 | cursor, codex |
| B4a 파서·모델 | 9 | 1769 | claude, cursor |
| B4b 비뷰 서비스 | 29 | 4058 | cursor, agy |

완료 통보는 조정자 터미널 `term_5874cf82-30f1-47c4-9f42-121e00921be4` 로 온다.
세션이 바뀌면 이 통보를 못 받는다. 그때는 각 worktree 의 `Docs/audit/` 를 직접 확인한다.

## 감사 결과 처리 절차
1. 짝(같은 블록 두 보고서)끼리 대조한다. 중복 = 신뢰도 신호, 불일치 = 재현으로 판정.
2. 이미 고친 것과 겹치는지 본다. 아래 "이미 고친 것" 참조.
3. 재현되는 것만 고친다. 파일 소유권을 겹치지 않게 나눠 병렬 위임한다.

## 이미 고친 것 — 다시 보고되면 제외
`--force-log`(E205005), `commit(paths: [])` 전체 커밋, NUL 메시지 크래시,
`export`/`move` peg revision 비대칭, URL NFC 강제 변환,
텍스트+속성 동시 충돌 백업 누락, 트리 충돌 하위 트리 삭제,
파괴적 확인창 Return, 커밋 중복 제출,
프로젝트 전환 시 상태 초기화 누락, 요청 객체 `projectID` 검증,
지역화 키 노출(이제 타입 검사 대상), `<conflict>` 요소 보존,
브라우저 캐시 무효화, 커밋 기록 복원 진입점, 테스트 타이밍 불안정,
커밋 재시도에서 삭제 예약 누락.

## 의도된 동작 — 버그로 보고돼도 되돌리지 말 것
- 문서 열기 확인의 Return 이 잠금 없이 여는 것. 커밋 `6fdca63` 이 의도적으로 그렇게 바꿨다.

## 남은 부채
1. `hdiutil detach` 정리 실패가 테스트 실패로 잡히는 구조.
   `realSVNCanonicalAliasRepairPreservesBytesAndNonAliasStatuses` 가 시뮬레이터 종료 중이면 빨간불이 된다.
   정리 실패는 경고로 낮추거나 재시도를 넣는다.
2. 테스트 60파일 16k줄은 이번 전수 감사 대상이 아니다. 각 담당이 "검증 공백" 절로만 다룬다.

## 참고 문서
- `Docs/SVNArgumentRules.md` — 명령별 인자 규칙표. 미확인 칸은 비워둔 상태다.
- `Docs/audit/` — 이전 감사 보고서 10건.
