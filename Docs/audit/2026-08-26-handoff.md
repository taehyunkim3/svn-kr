# svn-mac 세션 인계 메모 (2026-08-26 갱신)

## 현재 상태
- `master` 기준 `swift build` 무경고, `swift test` **638개 통과**(세션 시작 시 344개).
- worktree 정리 완료. 진행 중인 하위 에이전트 없음.

## 위임 규칙 (사용자 지시)
- **agy(antigravity)** — 코드 수정 금지. 읽기 전용 감사·조사만.
- **claude** — 사용자가 명시적으로 지목하지 않으면 하위 에이전트로 쓰지 않는다. 비용 때문.
  기본 위임 대상은 **codex**와 **cursor**. 두 번 반려되면 그때 claude 투입을 사용자에게 제안한다.

## 바로 착수할 수 있는 작업 2개

### 1. l10n 네임스페이스 전환 (브리프 준비 완료)
브리프: `/tmp/svnmac-briefs/l10n-namespace.txt`
사용자가 A안을 선택했다. **codex로 돌린다.**

문제: 지금 호출부가 `localized(.ui.revision.foo)` 형태의 **암시적 멤버 체인**이라
SourceKit-LSP가 완성 후보를 못 낸다. VSCode에서 `.ui.` 뒤 자동완성과 정의 이동이 안 된다.
도구 문제가 아니다. 인덱스 스토어와 `sourcekit-lsp`, Swift 6.3.3 모두 정상임을 확인했다.

목표: `L10n.UI.Revision.symlinkNotAllowed` 형태의 명시적 타입 경로.
케이스 없는 중첩 enum을 네임스페이스로, 키는 `static let`.

핵심 게이트:
- `sourcekit-lsp` completion으로 자동완성이 실제로 되는지 검증하고 결과 보고
- 문구 값 덤프 전후 비교 차이 0 (`scripts/dump-localization-call-values.swift` 재사용)
- `allCases`가 663줄 문자열을 런타임 `split`하는 퇴행 상태다. 타입 참조 배열로 되돌릴 것
- 리소스 키 값(`"ui.revision.foo"`)은 바꾸지 않는다. Swift 접근 경로와 이름만 바꾼다

### 2. 전수 감사 잔여 5건
`r3-display`가 허용 경로 밖이라 손대지 못한 항목이다. 그 worktree 보고를 확인하거나
`Docs/audit/2026-08-26-sweep-*.md`에서 아래 키워드로 찾는다.
- 속성 전용 충돌이 사이드바 빨간 뱃지에 안 잡힌다
- `svn propget --xml`의 `target/@path`가 절대경로라 무시 규칙 디렉터리가 절대경로가 된다
- 로그 리비전 속성의 `encoding="base64"`를 무시해 base64 원문을 보여준다
- `.alwaysLockAndOpen` 정책에서 타 사용자 잠금 파일이 경고 없이 쓰기 모드로 열린다
  (주의: 사용자가 명시적으로 고른 정책이다. 경고만 더할지 정책을 바꿀지 판단이 필요하다)
- `SVNConflictDetails`의 `conflicts` 기본값이 목록 유실을 컴파일 단계에서 숨긴다

## 알려진 부채
1. `hdiutil detach` 정리 실패가 테스트 실패로 잡힌다.
   `realSVNCanonicalAliasRepairPreservesBytesAndNonAliasStatuses`가
   시뮬레이터 종료 중이면 빨간불이 된다. 정리 실패는 경고로 낮추거나 재시도를 넣을 것.
2. `beginPathRecovery()`를 호출하는 화면이 없다. 복구 기능 전체가 도달 불가다.
   로직은 이번 세션에 여러 건 고쳤다. **버튼을 연결하기 전에 반드시 현재 상태를 재검증할 것.**
3. 테스트 60여 파일은 전수 감사 대상이 아니었다. 각 감사가 "검증 공백" 절로만 다뤘다.

## 의도된 동작 — 버그로 보고돼도 되돌리지 말 것
- 문서 열기 확인의 Return이 잠금 없이 여는 것. 커밋 `6fdca63`이 의도적으로 그렇게 바꿨다.

## 작업 절차
1. 브리프에 **허용 파일 목록**을 명시하고, 타입 정의 파일까지 `grep -rl`로 미리 확인해 포함시킨다.
   시그니처를 바꾸는 작업은 정의·호출·테스트가 한 묶음이다. 이 실수를 이번 세션에 4번 반복했다.
2. 게이트: `swift build` 무경고, `swift test` 기준선 이상 전부 통과, 허용 목록 밖 변경 0,
   **회귀 테스트가 수정을 되돌렸을 때 실제로 실패하는지 확인**하고 결과를 보고에 적게 한다.
3. 완료 통보는 조정자 터미널 핸들로 받는다. 세션이 바뀌면 핸들이 만료되므로
   그때는 worktree에 `DONE-<이름>.md` 파일로 남기게 지시한다.

## 참고 문서
- `Docs/SVNArgumentRules.md` — 명령별 인자 규칙표. 미확인 칸은 비워둔 상태.
- `Docs/audit/` — 감사 보고서 22건. 2026-08-25는 위험 축별, 2026-08-26은 전수 커버리지.
