# SVN Mac 작업 규칙

## 레이아웃 변경

- 주요 창, 사이드바, 작업 패널 크기는 `Sources/SVNMac/AppLayout.swift`만 수정한다. 화면 파일에 새 최소/이상 크기 숫자를 직접 추가하지 않는다.
- 변경 사항과 커밋 기록의 좌우 패널은 `WorkspaceSplitView`를 사용한다. 각 자식에 증상 해결용 `maxHeight: .infinity`를 반복해서 추가하지 않는다.
- 사용자가 직접 분할선을 조절해야 하는 요구가 없다면 중첩 `HSplitView`/`VSplitView`를 만들지 않는다. 고정 목록과 나머지 콘텐츠는 `VStack`과 `Divider`로 구성한다.
- 빈 상태, 로딩, 결과 상태가 바뀌어도 바깥 컨테이너 타입과 크기는 유지한다. 상태 안내는 가능한 한 `overlay`로 표시한다.
- 배경이 패널 전체를 채워야 하면 먼저 전체 크기 `frame`을 적용하고 그 다음 `background`를 적용한다.
- 레이아웃 변경 전후에 `Docs/LayoutArchitecture.md`의 창 크기·상태 조합을 확인하고 `swift test`를 실행한다.

## 변경 범위와 커밋

- 사용자가 만든 기존 변경과 스테이징을 보존한다. 관련 파일만 명시해 스테이징하고 커밋한다.
- 레이아웃 기반, 화면 적용, 회귀 방지 규칙은 가능한 한 별도 커밋으로 남겨 다른 세션이 변경 이유를 추적할 수 있게 한다.

## 릴리스 태그

- 제출용 빌드를 만들면 그 커밋에 annotated 태그를 단다. 형식은 `v<CFBundleShortVersionString>+<CFBundleVersion>` — 예: `v1.0.5+42`.
- 태그를 먼저 만들고, 그 태그를 체크아웃해서 `scripts/package-app-store.sh`를 실행한다. 빌드한 뒤에 소급해서 달지 않는다.
- 패키징 전에 `Resources/Info.plist`의 두 값이 태그와 일치하는지 확인한다. 어긋나면 패키징을 멈춘다.
- `CFBundleVersion`은 재사용하지 않는다. 심사에서 반려된 제출의 번호도 소비된 것으로 본다.
- 게시된 태그는 이동·삭제하지 않는다. 잘못 달았으면 다음 빌드 번호로 새로 단다.
- 태그 메시지에 제출 정보를 남긴다. 반려됐으면 사유 요약을 함께 적어 다음 제출에서 같은 항목을 다시 밟지 않는다.
- 스토어 화면과 릴리스 노트에는 `1.0.5(42)` 표기를 쓴다. 태그 이름에는 괄호를 쓰지 않는다. 괄호는 셸 메타문자라 `git push origin "v1.0.5(42)"` 처럼 매번 인용이 필요하고 CI의 semver 패턴에도 걸리지 않는다.
